use State;
use Direction;
use Transition;
use Tape;

#[derive[Debug]]
pub struct TM {
    // Tape 
    // State
    state: State,
    pos: uszie,      
    tape: Tape
}


impl TM {
    fn new() -> Self {

    }
}

